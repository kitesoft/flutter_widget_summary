class AlignDoc{
  ///这是一个只能设置子View对齐方式的容器，没有其他可定制的元素
  ///constructor params:
  ///{
  /// child : 类型widget，子View
  /// widthFactor ： 类型double，如果非null，将其宽度设置为子元素的宽度乘以这个因数。
  /// heightFactor ： 类型double，作用同widthFactor
  /// alignment : 类型AlignmentGeometry，跟android里RelativeLayout里的align系列属性相似，控制子view的对齐位置
  ///}
}