class OverflowBoxDoc{
  ///允许child超出parent的范围显示
  ///constructor params:
  ///{
  /// maxWidth : 类型double，最大宽度
  /// maxHeight ：类型double，最大高度
  /// minWidth ：类型double，最小宽度
  /// minHeight ：类型double，最小高度
  /// alignment ：类型AlignmentGeometry，跟android里RelativeLayout里的align系列属性相似，控制子view的对齐位置
  ///}
}